import { createStore } from "vuex";

export default createStore({
  state: {
    
  },
  mutations: {
    // addCartItem(state, item) {
    //   item.quantity = 1;
    //   state.cart.push(item);
    // },
  }
});